{
  "name": "aurora-statusline",
  "version": "3.1.0",
  "type": "module",
  "description": "Claude Code status line component — model badge, git info, context window, rate limits, monthly cost",
  "bin": {
    "aurora-statusline": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc && echo '{\"type\":\"commonjs\"}' > dist/package.json",
    "start": "node dist/index.js",
    "setup": "node dist/index.js --setup",
    "test": "node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3"
  }
}
